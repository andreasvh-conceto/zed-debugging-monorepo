{
  "name": "zed-debugging-monorepo",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm i --prefix backend",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.7"
  }
}
